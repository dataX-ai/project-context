"""
Delete Context MCP Tool
"""

# from mcp.server import Server


def register_delete_context_tool(server, context_manager):
    """Register the delete_context MCP tool"""
    
    # @server.tool("delete_context")
    # async def delete_context(topic: str, category: str = None, confirm: bool = False) -> dict:
    #     """Remove contexts about a specific topic"""
    #     try:
    #         result = await context_manager.delete_contexts_by_topic(topic, category)
    #         return {
    #             "success": True,
    #             "deleted_contexts": result['deleted_contexts'],
    #             "count": len(result['deleted_contexts'])
    #         }
    #     except Exception as e:
    #         return {
    #             "success": False,
    #             "error": str(e)
    #         }
    
    # TODO: Implement delete_context tool
    pass 